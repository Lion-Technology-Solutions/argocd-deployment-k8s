apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deli.fullname" . }}-test-connection"
  labels:
    {{- include "deli.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deli.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
