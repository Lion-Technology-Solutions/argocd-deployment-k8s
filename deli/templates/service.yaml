apiVersion: v1
kind: Service
metadata:
  name: {{ include "deli.fullname" . }}
  labels:
    {{- include "deli.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deli.selectorLabels" . | nindent 4 }}
